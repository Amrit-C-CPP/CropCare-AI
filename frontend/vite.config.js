import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'
import { VitePWA } from 'vite-plugin-pwa'

// https://vite.dev/config/
export default defineConfig({
  plugins: [
    react(), 
    tailwindcss(),
    VitePWA({
      registerType: 'autoUpdate',
      includeAssets: ['favicon.svg', 'icons.svg'],
      manifest: {
        name: 'KrishiSalakar — AI Crop Disease Detection',
        short_name: 'KrishiSalakar',
        description: 'Empower your farm with precision agriculture. Scan leaves, detect diseases instantly, and receive actionable, data-driven treatment plans to maximize your yield.',
        theme_color: '#10b981', // Emerald green, fitting for agriculture
        background_color: '#ffffff',
        display: 'standalone',
        icons: [
          {
            src: '/favicon.svg',
            sizes: 'any',
            type: 'image/svg+xml',
            purpose: 'any maskable'
          }
        ]
      }
    })
  ],
})
